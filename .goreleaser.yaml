---
# GoReleaser configuration
# Documentation: https://goreleaser.com/customization/
version: 2

# Set the project name
project_name: f2b

# Clean dist folder before build
before:
  hooks:
    - go mod tidy
    - go generate ./...

# Build configuration
builds:
  - id: f2b
    main: .
    binary: f2b

    # Custom ldflags
    ldflags:
      - -s -w
      - -X github.com/ivuorinen/f2b/cmd.version={{.Version}}
      - -X github.com/ivuorinen/f2b/cmd.commit={{.Commit}}
      - -X github.com/ivuorinen/f2b/cmd.date={{.Date}}
      - -X github.com/ivuorinen/f2b/cmd.builtBy=goreleaser

    # Build for multiple platforms
    goos:
      - linux
      - darwin
      - freebsd
      - openbsd
      - netbsd

    goarch:
      - amd64
      - arm64
      - arm
      - 386

    goarm:
      - 6
      - 7

    # Skip certain combinations
    ignore:
      - goos: darwin
        goarch: 386
      - goos: darwin
        goarch: arm
      - goos: freebsd
        goarch: arm
      - goos: openbsd
        goarch: arm
      - goos: netbsd
        goarch: arm

    # Set environment variables
    env:
      - CGO_ENABLED=0

    # Custom build tags
    tags:
      - netgo
      - osusergo

# Archive configuration
archives:
  - id: f2b
    name_template: >-
      {{- .ProjectName }}_ {{- .Version }}_ {{- title .Os }}_ {{- if eq .Arch "amd64" }}x86_64 {{- else if eq .Arch "386"
      }}i386 {{- else }}{{ .Arch }}{{ end }} {{- if .Arm }}v{{ .Arm }}{{ end }}

    # Archive format
    format_overrides:
      - goos: windows
        format: zip

    # Files to include in archive
    files:
      - LICENSE.md
      - README.md
      - CHANGELOG.md
      - docs/*

# Checksum configuration
checksum:
  name_template: 'checksums.txt'
  algorithm: sha256

# Snapshot configuration
snapshot:
  name_template: "{{ incpatch .Version }}-next"

# Release configuration
release:
  github:
    owner: ivuorinen
    name: f2b

  # Release notes
  header: |
    ## f2b v{{ .Version }} ({{ .Date }})

    A modern, secure Go-based CLI tool for managing Fail2Ban jails and bans.

  footer: |
    ## Installation

    ### Using Go
    ```bash
    go install github.com/ivuorinen/f2b@latest
    ```

    ### Using Homebrew (macOS/Linux)
    ```bash
    brew tap ivuorinen/tap
    brew install f2b
    ```

    ### Manual Download
    Download the appropriate binary for your platform from the assets below.

    ## Documentation

    See the [README](https://github.com/ivuorinen/f2b#readme) for usage instructions.

  # Automatically generate release notes
  use_package_registry: true
  make_latest: true

# Changelog configuration
changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
      - 'typo'
      - 'Merge pull request'
      - 'Merge branch'
  groups:
    - title: '🚀 Features'
      regexp: '^feat'
    - title: '🐛 Bug Fixes'
      regexp: '^fix'
    - title: '🔒 Security'
      regexp: '^security'
    - title: '⚡ Performance'
      regexp: '^perf'
    - title: '♻️ Refactoring'
      regexp: '^refactor'
    - title: 'Other changes'

# Homebrew tap configuration
brews:
  - name: f2b
    repository:
      owner: ivuorinen
      name: homebrew-tap
      branch: main
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"

    commit_author:
      name: goreleaserbot
      email: bot@goreleaser.com

    commit_msg_template: "Brew formula update for {{ .ProjectName }} version {{ .Tag }}"

    homepage: "https://github.com/ivuorinen/f2b"
    description: "Modern, secure Go-based CLI tool for managing Fail2Ban jails and bans"
    license: "MIT"

    dependencies:
      - name: go
        type: optional

    test: |
      system "#{bin}/f2b", "version"

    install: |
      bin.install "f2b"

# NFPM configuration for Linux packages
nfpms:
  - id: f2b
    package_name: f2b

    vendor: ivuorinen
    homepage: https://github.com/ivuorinen/f2b
    maintainer: ivuorinen
    description: Modern, secure Go-based CLI tool for managing Fail2Ban jails and bans
    license: MIT

    formats:
      - deb
      - rpm
      - apk

    bindir: /usr/bin

    contents:
      - src: ./LICENSE.md
        dst: /usr/share/doc/f2b/LICENSE.md
      - src: ./README.md
        dst: /usr/share/doc/f2b/README.md

    scripts:
      postinstall: |
        #!/bin/sh
        echo "f2b has been installed. Run 'f2b --help' to get started."

# Docker configuration
dockers:
  - image_templates:
      - "ghcr.io/ivuorinen/f2b:{{ .Tag }}"
      - "ghcr.io/ivuorinen/f2b:v{{ .Major }}"
      - "ghcr.io/ivuorinen/f2b:v{{ .Major }}.{{ .Minor }}"
      - "ghcr.io/ivuorinen/f2b:latest"

    dockerfile: |
      FROM alpine:latest
      RUN apk --no-cache add ca-certificates
      COPY f2b /usr/local/bin/
      ENTRYPOINT ["f2b"]

    build_flag_templates:
      - "--pull"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.title={{.ProjectName}}"
      - "--label=org.opencontainers.image.revision={{.FullCommit}}"
      - "--label=org.opencontainers.image.version={{.Version}}"
      - "--label=org.opencontainers.image.source={{.GitURL}}"
      - "--platform=linux/amd64"

# Announce releases
announce:
  skip: false
