---
# Configuration file for MegaLinter
# See all available variables at
# https://megalinter.io/configuration/ and in linters documentation

APPLY_FIXES: all
SHOW_ELAPSED_TIME: false # Show elapsed time at the end of MegaLinter run
PARALLEL: true
VALIDATE_ALL_CODEBASE: true
FILEIO_REPORTER: false # Generate file.io report
GITHUB_STATUS_REPORTER: true # Generate GitHub status report
IGNORE_GENERATED_FILES: true # Ignore generated files
PRINT_ALPACA: false # Print Alpaca logo in console
SARIF_REPORTER: true # Generate SARIF report
SHOW_SKIPPED_LINTERS: false # Show skipped linters in MegaLinter log

# Exclude generated (docs/api/), vendored (.claude/skills/), and
# machine-managed (docs/audit/) trees from all linters. docs/api.md (the
# hand-written guide) is intentionally NOT matched — only the docs/api/ dir.
FILTER_REGEX_EXCLUDE: '(\.serena/|docs/api/|\.claude/skills/|docs/audit/)'

DISABLE_LINTERS:
  - REPOSITORY_DEVSKIM
  - GO_REVIVE # run as part of golangci-lint
  - GO_GOLANGCI_LINT # stuck in go version 1.24
  - JSON_V8R # not needed
  - YAML_V8R # not needed
  - MARKDOWN_MARKDOWN_TABLE_FORMATTER # conflicts with mdformat's table style

ACTION_ZIZMOR_UNSECURED_ENV_VARIABLES:
  - GITHUB_TOKEN
