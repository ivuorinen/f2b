---
name: Sync labels

# yamllint disable-line rule:truthy
on:
  push:
    branches:
      - main
    paths:
      - .github/labels.yml
  schedule:
    - cron: "34 5 * * *"
  workflow_call:
  workflow_dispatch:

jobs:
  SyncLabels:
    permissions:
      issues: write
    uses: ivuorinen/ivuorinen/.github/workflows/sync-labels.yml@26743e244eaea70c83c259370349ff3dd11d215b # 2025.7.0
